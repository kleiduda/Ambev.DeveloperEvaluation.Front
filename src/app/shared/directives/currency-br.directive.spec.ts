import { CurrencyBrDirective } from './currency-br.directive';

describe('CurrencyBrDirective', () => {
  it('should create an instance', () => {
    const directive = new CurrencyBrDirective();
    expect(directive).toBeTruthy();
  });
});
